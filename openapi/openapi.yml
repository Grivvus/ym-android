openapi: 3.0.3
info:
  title: YM Music Service
  version: "0.1.0"

paths:
  /ping:
    get:
      security: []
      summary: Checks whether the server is alive.
      operationId: ping
      responses:
        '200':
          description: The server is alive.

  /auth/login:
    post:
      security: []
      summary: Logs a user into their account.
      operationId: login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserAuth'
      responses:
        '200':
          description: The user was logged in successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '401':
          description: Invalid user credentials.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/register:
    post:
      security: []
      summary: Registers a new user.
      operationId: register
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserAuth'
      responses:
        '201':
          description: A new user account was created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '409':
          description: A user with these credentials already exists.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/refresh:
    post:
      security: []
      summary: Refreshes JWT tokens.
      operationId: refreshTokens
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTokenRequest'
      responses:
        '200':
          description: Tokens were refreshed successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '401':
          description: Invalid or expired refresh token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/password-reset/request:
    post:
      security: []
      summary: Requests a password reset code by email.
      operationId: requestPasswordReset
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordResetRequest'
      responses:
        '202':
          description: The request was accepted regardless of whether the email exists.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
        '400':
          description: Invalid email format or malformed request body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: Password reset is unavailable because email delivery is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/password-reset/confirm:
    post:
      security: []
      summary: Resets a password using the code sent by email.
      operationId: confirmPasswordReset
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PasswordResetConfirmRequest'
      responses:
        '200':
          description: The password was reset successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
        '400':
          description: Invalid request body, invalid code, or expired code.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: Password reset is unavailable because email delivery is not configured.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /users:
    get:
      summary: Gets all user IDs and usernames.
      operationId: getAllUsers
      responses:
        '200':
          description: Users were fetched successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Users'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /users/{userId}:
    get:
      summary: Gets a user by ID.
      operationId: getUser
      parameters:
        - in: path
          name: userId
          schema:
            type: integer
            format: int32
          required: true
          description: User ID represented as an integer.
      responses:
        '200':
          description: User data was fetched successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserReturn'
        '401':
          description: Unauthorized access.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: The user does not have access to this information.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      summary: Updates a user.
      operationId: changeUser
      parameters:
        - in: path
          name: userId
          schema:
            type: integer
            format: int32
          required: true
          description: User ID represented as an integer.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserUpdate'
      responses:
        '200':
          description: The user was updated successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserReturn'
        '400':
          description: Invalid request data.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: You do not have permission to modify this user.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /users/{userId}/simple:
    get:
      summary: Gets a user by ID.
      operationId: getSimpleUser
      parameters:
        - in: path
          name: userId
          schema:
            type: integer
            format: int32
          required: true
          description: User ID represented as an integer.
      responses:
        '200':
          description: User data was fetched successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimpleUser'
        '401':
          description: Unauthorized access.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /users/{userId}/avatar:
    get:
      summary: Gets the uploaded user avatar.
      operationId: getUserAvatar
      parameters:
        - in: path
          name: userId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: The avatar image was returned successfully.
          content:
            image/webp:
              schema:
                type: string
                format: binary
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Avatar not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Uploads a new user avatar to the server.
      operationId: uploadUserAvatar
      parameters:
        - in: path
          name: userId
          schema:
            type: integer
            format: int32
          required: true
      requestBody:
        required: true
        content:
          image/webp:
            schema:
              type: string
              format: binary
      responses:
        '201':
          description: The avatar was uploaded successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
        '400':
          description: Invalid image format.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: You can not modify this user's avatar.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      summary: Deletes the current avatar.
      operationId: deleteUserAvatar
      parameters:
        - in: path
          name: userId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: The avatar was deleted successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: You can not modify this user's avatar.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /users/{userId}/change_password:
    patch:
      summary: Changes a user's password.
      operationId: changePassword
      parameters:
        - in: path
          name: userId
          schema:
            type: integer
            format: int32
          required: true
          description: User ID represented as an integer.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserChangePassword'
      responses:
        '200':
          description: The user's password was changed successfully.
        '400':
          description: Invalid old password.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: You can not modify this user's password

  /tracks:
    post:
      summary: Uploads a new track, queues transcoding, and stores it.
      operationId: uploadTrack
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/TrackUploadRequest'
      responses:
        '202':
          description: The track was uploaded and accepted into the transcoding queue.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrackUploadSuccessResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '415':
          description: Unsupported media type. The uploaded file is not a supported audio file.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    get:
      summary: Gets metadata for all uploaded tracks.
      operationId: getTracks
      responses:
        '200':
          description: Tracks were fetched successfully.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TrackMetadata'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /tracks/{trackId}:
    delete:
      summary: Deletes a track.
      operationId: deleteTrack
      parameters:
        - in: path
          name: trackId
          schema:
            type: integer
            format: int32
          required: true
          description: Track ID represented as an integer.
      responses:
        '200':
          description: The track was deleted successfully.
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: The user can not delete this track.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Track not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    get:
      summary: Gets track metadata.
      operationId: getTrackMeta
      parameters:
        - in: path
          name: trackId
          schema:
            type: integer
            format: int32
          required: true
          description: Track ID represented as an integer.
      responses:
        '200':
          description: Track metadata was fetched successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrackMetadata'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: The user does not have access to this track.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Track not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /tracks/{id}/stream:
    head:
      summary: Gets track metadata required for streaming.
      operationId: streamTrackHead
      parameters:
        - in: path
          name: id
          schema:
            type: integer
            format: int32
          required: true
        - in: query
          name: quality
          schema:
            type: string
          description: Preferred quality. If unavailable, the server falls back to the closest available preset, then to the original track file.
          required: false
      responses:
        '200':
          description: Streaming metadata was fetched successfully.
          headers:
            Content-Type:
              description: Media type of the track file.
              schema:
                type: string
              example: audio/mpeg
            Content-Length:
              description: Full streamable file size in bytes.
              schema:
                type: integer
                format: int64
              example: 8423912
            Accept-Ranges:
              description: Indicates that byte-range requests are supported.
              schema:
                type: string
                enum: [bytes]
              example: bytes
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: The user does not have access to this track.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Track not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    get:
      summary: Streams a track.
      operationId: streamTrack
      parameters:
        - in: path
          name: id
          schema:
            type: integer
            format: int32
          required: true
        - in: query
          name: quality
          schema:
            type: string
          description: Preferred quality. If unavailable, the server falls back to the closest available preset, then to the original track file.
          required: false
      responses:
        '200':
          description: The track stream was returned successfully.
          content:
            audio/*:
              schema:
                type: string
                format: binary
        '206':
          description: Partial content was returned successfully.
          content:
            audio/*:
              schema:
                type: string
                format: binary
        '416':
          description: Invalid byte range.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: The user does not have access to this track.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Track not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /tracks/{id}/download:
    head:
      summary: Gets track metadata required for download.
      operationId: downloadTrackHead
      parameters:
        - in: path
          name: id
          schema:
            type: integer
            format: int32
          required: true
        - in: query
          name: quality
          schema:
            type: string
          description: Preferred quality. If unavailable, the server falls back to the closest available preset, then to the original track file.
          required: false
      responses:
        '200':
          description: Download metadata was fetched successfully.
          headers:
            Content-Type:
              description: Media type of the downloadable track file.
              schema:
                type: string
              example: audio/mpeg
            Content-Length:
              description: Full downloadable file size in bytes.
              schema:
                type: integer
                format: int64
              example: 8423912
            Accept-Ranges:
              description: Indicates that byte-range requests are supported.
              schema:
                type: string
                enum: [bytes]
              example: bytes
            ETag:
              description: Opaque identifier for this exact downloadable file version.
              schema:
                type: string
              example: '"track-123-standard-v5"'
            Content-Disposition:
              description: Suggested filename for saving the track.
              schema:
                type: string
              example: attachment; filename*=UTF-8''artist-title.mp3
            X-Track-Quality-Requested:
              description: Quality requested by the client.
              schema:
                type: string
                enum: [fast, standard, high, lossless]
              example: standard
            X-Track-Quality-Resolved:
              description: Actual quality selected by the server.
              schema:
                type: string
                enum: [fast, standard, high, lossless, original]
              example: standard
            X-Track-Checksum-Sha256:
              description: SHA-256 checksum of the full downloadable file.
              schema:
                type: string
              example: 0b4f0c7a...
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: The user does not have access to this track.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Track not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    get:
      summary: Downloads a track file.
      operationId: downloadTrack
      parameters:
        - in: path
          name: id
          schema:
            type: integer
            format: int32
          required: true
        - in: query
          name: quality
          schema:
            type: string
          description: Preferred quality. If unavailable, the server falls back to the closest available preset, then to the original track file.
          required: false
      responses:
        '200':
          description: The track file was returned successfully.
          content:
            audio/*:
              schema:
                type: string
                format: binary
        '206':
          description: Partial content was returned successfully.
          content:
            audio/*:
              schema:
                type: string
                format: binary
        '416':
          description: Invalid byte range.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: The user does not have access to this track.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Track not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /artists:
    post:
      summary: Creates a new artist.
      operationId: createArtist
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ArtistCreateRequest'
      responses:
        '201':
          description: The artist was created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArtistCreateResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Conflict. The artist already exists.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    get:
      summary: Gets a list of all artists.
      operationId: getAllArtists
      parameters:
        - in: query
          name: starts_with
          schema:
            type: string
          description: Query parameter for searching or filtering artists.
        - in: query
          name: limit
          schema:
            type: integer
          description: Maximum number of artists to return.
      responses:
        '200':
          description: Artists were fetched successfully.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ArtistInfoResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /artists/{artistId}:
    get:
      summary: Gets artist information.
      operationId: getArtist
      parameters:
        - in: path
          name: artistId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: Artist information was fetched successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArtistInfoResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Artist not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      summary: Deletes an artist by ID.
      operationId: deleteArtist
      parameters:
        - in: path
          name: artistId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: The artist was deleted successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArtistDeleteResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /artists/{artistId}/image:
    post:
      description: Uploads a new image for the artist.
      operationId: uploadArtistImage
      parameters:
        - in: path
          name: artistId
          schema:
            type: integer
            format: int32
          required: true
      requestBody:
        content:
          image/*:
            schema:
              type: string
              format: binary
      responses:
        '201':
          description: The artist image was uploaded successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArtistImageResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Artist not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      description: Deletes the artist image. If no image exists, the operation is a no-op.
      operationId: deleteArtistImage
      parameters:
        - in: path
          name: artistId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: The artist image was deleted successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArtistImageResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Artist not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    get:
      description: Fetches the artist image.
      operationId: getArtistImage
      parameters:
        - in: path
          name: artistId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: The artist image was fetched successfully.
          content:
            image/webp:
              schema:
                type: string
                format: binary
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Artist not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /playlists:
    post:
      summary: Creates a new playlist.
      operationId: createPlaylist
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PlaylistCreateRequest'
      responses:
        '201':
          description: The playlist was created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistCreateResponse'
        '400':
          description: Required parameters are missing.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Conflict. A playlist with this name already exists.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    get:
      summary: Gets all playlists available to the user.
      operationId: getPlaylists
      parameters:
        - in: query
          name: include_public
          schema:
            type: boolean
        - in: query
          name: include_shared
          schema:
            type: boolean
        - in: query
          name: include_owned
          schema:
            type: boolean
      responses:
        '200':
          description: Playlists were fetched successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Playlists'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /playlists/{playlistId}:
    post:
      summary: Adds a track to a playlist.
      operationId: addTrackToPlaylist
      parameters:
        - in: path
          name: playlistId
          schema:
            type: integer
            format: int32
          required: true
      requestBody:
        content:
          application/json:
            schema:
              type: object
              required:
                - track_id
              properties:
                track_id:
                  type: integer
                  format: int32
      responses:
        '200':
          description: The track was added to the playlist successfully.
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: You do not have permission to modify this playlist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Playlist not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      summary: Updates a playlist.
      operationId: updatePlaylist
      parameters:
        - in: path
          name: playlistId
          schema:
            type: integer
            format: int32
          required: true
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaylistUpdateRequest'
      responses:
        '200':
          description: The playlist was updated successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: You do not have permission to modify this playlist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Playlist not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    get:
      summary: Gets playlist information.
      operationId: getPlaylist
      parameters:
        - in: path
          name: playlistId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: Playlist data was fetched successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistWithTracksResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: You can not have access to this playlist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Playlist not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      summary: Deletes a playlist by ID.
      operationId: deletePlaylist
      parameters:
        - in: path
          name: playlistId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: The playlist was deleted successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistDeleteResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: You do not have permission to modify this playlist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Playlist not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /playlists/{playlistId}/tracks/{trackId}:
    delete:
      summary: Removes a track from a playlist.
      operationId: deleteTrackFromPlaylist
      parameters:
        - in: path
          name: playlistId
          schema:
            type: integer
            format: int32
          required: true
        - in: path
          name: trackId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: The track was removed from the playlist, or the link did not exist.
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: You do not have permission to modify this playlist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Playlist not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /playlists/{playlistId}/share:
    post:
      description: Shares this playlist with other users.
      operationId: sharePlaylist
      parameters:
        - in: path
          name: playlistId
          required: true
          schema:
            type: integer
            format: int32
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaylistShareRequest'
      responses:
        '200':
          description: The playlist was shared with the specified users.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: You do not have permission to modify this playlist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Playlist not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /playlists/{playlistId}/revoke:
    post:
      description: Revokes access to the playlist.
      operationId: revokePlaylist
      parameters:
        - in: path
          name: playlistId
          required: true
          schema:
            type: integer
            format: int32
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlaylistRevokeAccessRequest'
      responses:
        '200':
          description: Access was revoked successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: You do not have permission to modify this playlist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Playlist not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /playlists/{playlistId}/cover:
    post:
      description: Uploads a new cover image for the playlist.
      operationId: uploadPlaylistCover
      parameters:
        - in: path
          name: playlistId
          schema:
            type: integer
            format: int32
          required: true
      requestBody:
        content:
          image/*:
            schema:
              type: string
              format: binary
      responses:
        '201':
          description: The playlist cover was uploaded successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistCoverResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: You do not have permission to modify this playlist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Playlist not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      description: Deletes the playlist cover. If no cover exists, the operation is a no-op.
      operationId: deletePlaylistCover
      parameters:
        - in: path
          name: playlistId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: The playlist cover was deleted successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaylistCoverResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: You do not have permission to modify this playlist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Playlist not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    get:
      description: Fetches the playlist cover image.
      operationId: getPlaylistCover
      parameters:
        - in: path
          name: playlistId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: The playlist cover was fetched successfully.
          content:
            image/webp:
              schema:
                type: string
                format: binary
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Playlist not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /albums:
    post:
      summary: Creates a new album.
      operationId: createAlbum
      requestBody:
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AlbumCreateRequest'
      responses:
        '201':
          description: The album was created successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlbumCreateResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Conflict. The artist already has an album with this name.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /albums/{albumId}:
    get:
      summary: Gets album information.
      operationId: getAlbum
      parameters:
        - in: path
          name: albumId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: Album data was fetched successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlbumInfoResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Album not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      summary: Deletes an album by ID.
      operationId: deleteAlbum
      parameters:
        - in: path
          name: albumId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: The album was deleted successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlbumDeleteResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Superuser access required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /albums/{albumId}/tracks/{trackId}:
    delete:
      summary: Removes a track from an album.
      operationId: deleteTrackFromAlbum
      parameters:
        - in: path
          name: albumId
          schema:
            type: integer
            format: int32
          required: true
        - in: path
          name: trackId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: The track was removed from the album, or the link did not exist.
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Superuser access required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Album not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /albums/{albumId}/cover:
    post:
      description: Uploads a new cover image for the album.
      operationId: uploadAlbumCover
      parameters:
        - in: path
          name: albumId
          schema:
            type: integer
            format: int32
          required: true
      requestBody:
        content:
          image/*:
            schema:
              type: string
              format: binary
      responses:
        '201':
          description: The album cover was uploaded successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlbumCoverResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Album not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      description: Deletes the album cover. If no cover exists, the operation is a no-op.
      operationId: deleteAlbumCover
      parameters:
        - in: path
          name: albumId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: The album cover was deleted successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlbumCoverResponse'
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Album not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    get:
      description: Fetches the album cover image.
      operationId: getAlbumCover
      parameters:
        - in: path
          name: albumId
          schema:
            type: integer
            format: int32
          required: true
      responses:
        '200':
          description: The album cover was fetched successfully.
          content:
            image/webp:
              schema:
                type: string
                format: binary
        '401':
          description: Unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Album not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /backup:
    post:
      description: Starts a full application backup.
      operationId: backup
      parameters:
        - in: query
          name: include_images
          schema:
            type: boolean
        - in: query
          name: include_transcoded_tracks
          schema:
            type: boolean
      responses:
        '202':
          description: The backup operation was started.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BackupStatusResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Superuser access required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: A backup operation has already been started.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /backup/{backupId}:
    get:
      description: Gets the status of a backup operation.
      operationId: getBackupStatus
      parameters:
        - in: path
          name: backupId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Backup status was fetched successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BackupStatusResponse'
        '400':
          description: Invalid backup state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Superuser access required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Backup operation not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /backup/{backupId}/download:
    get:
      description: Downloads a finished backup archive.
      operationId: downloadBackup
      parameters:
        - in: path
          name: backupId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: The backup archive was returned successfully.
          headers:
            Content-Disposition:
              description: Suggested filename for the backup archive.
              schema:
                type: string
                example: attachment; filename="ym-backup-2026-04-12T10-00-00Z.zip"
            Cache-Control:
              description: Prevents caching of the backup archive.
              schema:
                type: string
                example: no-store
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '400':
          description: Invalid backup state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Superuser access required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Backup operation not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Backup operation is not finished yet.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /restore:
    post:
      description: Restores application data from a backup archive.
      operationId: restore
      requestBody:
        required: true
        content:
          application/zip:
            schema:
              type: string
              format: binary
      responses:
        '202':
          description: The archive was received and validated.
          content:
            application/json:
              schema:
                type: string
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Superuser access required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: A restore operation has already been started.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /restore/{restoreId}:
    get:
      description: Gets the status of a restore operation.
      operationId: getRestoreStatus
      parameters:
        - in: path
          name: restoreId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Restore status was fetched successfully.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RestoreStatusResponse'
        '400':
          description: Invalid restore state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Authentication required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Superuser access required.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Restore operation not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    OperationStatus:
      type: string
      enum:
        - pending
        - started
        - finished
        - error

    BackupStatusResponse:
      type: object
      required:
        - backup_id
        - status
        - include_images
        - include_transcoded_tracks
      properties:
        backup_id:
          type: string
        status:
          $ref: '#/components/schemas/OperationStatus'
        include_images:
          type: boolean
        include_transcoded_tracks:
          type: boolean
        size_bytes:
          type: integer
          format: int64
        error:
          type: string
          nullable: true

    RestoreStatusResponse:
      type: object
      required:
        - restore_id
        - status
      properties:
        restore_id:
          type: string
        status:
          $ref: '#/components/schemas/OperationStatus'
        error:
          type: string
          nullable: true

    UserAuth:
      type: object
      required:
        - username
        - password
      properties:
        username:
          type: string
        password:
          type: string
          format: password

    SimpleUser:
      type: object
      required:
        - id
        - username
      properties:
        id:
          type: integer
          format: int32
        username:
          type: string

    Users:
      type: array
      items:
        $ref: '#/components/schemas/SimpleUser'

    UserReturn:
      type: object
      required:
        - username
        - email
        - id
        - is_superuser
      properties:
        id:
          type: integer
          format: int32
        username:
          type: string
        is_superuser:
          type: boolean
        email:
          type: string
          nullable: true

    UserChangePassword:
      type: object
      required:
        - old_password
        - new_password
      properties:
        old_password:
          type: string
        new_password:
          type: string

    PasswordResetRequest:
      type: object
      required:
        - email
      properties:
        email:
          type: string
          format: email

    PasswordResetConfirmRequest:
      type: object
      required:
        - email
        - code
        - new_password
      properties:
        email:
          type: string
          format: email
        code:
          type: string
          minLength: 6
        new_password:
          type: string
          format: password
          minLength: 6

    UserUpdate:
      type: object
      properties:
        new_username:
          type: string
        new_email:
          type: string
          format: email

    UpdateTokenRequest:
      type: object
      required:
        - refresh_token
      properties:
        refresh_token:
          type: string

    TokenResponse:
      type: object
      required:
        - user_id
        - token_type
        - access_token
        - refresh_token
      properties:
        user_id:
          type: integer
          format: int32
        token_type:
          type: string
        access_token:
          type: string
        refresh_token:
          type: string

    ErrorResponse:
      type: object
      required:
        - error
      properties:
        error:
          type: string

    MessageResponse:
      type: object
      required:
        - msg
      properties:
        msg:
          type: string

    TrackUploadSuccessResponse:
      type: object
      required:
        - track_id
      properties:
        track_id:
          type: integer
          format: int32

    TrackUploadRequest:
      type: object
      required:
        - name
        - artist_id
        - track
      properties:
        name:
          type: string
        album_id:
          type: integer
          format: int32
        is_single:
          type: boolean
        is_globally_available:
          type: boolean
        artist_id:
          type: integer
          format: int32
        track:
          type: string
          format: binary

    TrackMetadata:
      type: object
      required:
        - track_id
        - name
        - artist_id
        - album_id
        - duration_ms
      properties:
        track_id:
          type: integer
          format: int32
        name:
          type: string
        artist_id:
          type: integer
          format: int32
        album_id:
          type: integer
          format: int32
        duration_ms:
          type: integer
          format: int32
        track_fast_preset:
          type: string
        track_standard_preset:
          type: string
        track_high_preset:
          type: string
        track_lossless_preset:
          type: string

    ArtistCreateRequest:
      type: object
      required:
        - artist_name
      properties:
        artist_name:
          type: string
        artist_image:
          type: string
          format: binary

    ArtistCreateResponse:
      type: object
      required:
        - artist_id
        - cover_uploaded
      properties:
        artist_id:
          type: integer
          format: int32
        cover_uploaded:
          type: boolean

    ArtistInfoResponse:
      type: object
      required:
        - artist_id
        - artist_name
        - artist_albums
      properties:
        artist_id:
          type: integer
          format: int32
        artist_name:
          type: string
        artist_albums:
          type: array
          items:
            type: integer
            format: int32

    ArtistDeleteResponse:
      type: object
      required:
        - artist_id
      properties:
        artist_id:
          type: integer
          format: int32

    ArtistImageResponse:
      type: object
      required:
        - artist_id
      properties:
        artist_id:
          type: integer
          format: int32

    PlaylistCreateRequest:
      type: object
      required:
        - owner_id
        - playlist_name
        - is_public
      properties:
        owner_id:
          type: integer
          format: int32
        playlist_name:
          type: string
        is_public:
          type: boolean
        playlist_cover:
          type: string
          format: binary

    PlaylistCreateResponse:
      type: object
      required:
        - playlist_id
        - cover_uploaded
      properties:
        playlist_id:
          type: integer
          format: int32
        cover_uploaded:
          type: boolean

    PlaylistWithTracksResponse:
      type: object
      required:
        - playlist_id
        - playlist_name
        - tracks
        - shared_with
      properties:
        playlist_id:
          type: integer
          format: int32
        playlist_name:
          type: string
        tracks:
          type: array
          items:
            type: integer
            format: int32
        shared_with:
          type: array
          items:
            type: integer
            format: int32

    PlaylistUpdateRequest:
      type: object
      required:
        - playlist_name
      properties:
        playlist_name:
          type: string

    PlaylistShareRequest:
      type: object
      required:
        - share_with_users
        - has_write_permission
      properties:
        share_with_users:
          type: array
          items:
            type: integer
            format: int32
        has_write_permission:
          type: boolean

    PlaylistRevokeAccessRequest:
      type: object
      required:
        - user_id
      properties:
        user_id:
          type: integer
          format: int32

    PlaylistResponse:
      type: object
      required:
        - playlist_id
        - playlist_name
      properties:
        playlist_id:
          type: integer
          format: int32
        playlist_name:
          type: string

    ExtendedPlaylist:
      type: object
      required:
        - playlist_id
        - playlist_name
        - playlist_owner_id
        - playlist_type
      properties:
        playlist_id:
          type: integer
          format: int32
        playlist_name:
          type: string
        playlist_owner_id:
          type: integer
          format: int32
        playlist_type:
          type: string
          enum: [owned, public, shared]

    Playlists:
      type: array
      items:
        $ref: '#/components/schemas/ExtendedPlaylist'

    PlaylistDeleteResponse:
      type: object
      required:
        - playlist_id
      properties:
        playlist_id:
          type: integer
          format: int32

    PlaylistCoverResponse:
      type: object
      required:
        - playlist_id
      properties:
        playlist_id:
          type: integer
          format: int32

    AlbumCreateRequest:
      type: object
      required:
        - artist_id
        - album_name
      properties:
        artist_id:
          type: integer
          format: int32
        album_name:
          type: string
        album_cover:
          type: string
          format: binary
        release_year:
          type: integer
          format: int32
        release_full_date:
          type: string
          format: date

    AlbumCreateResponse:
      type: object
      required:
        - album_id
        - cover_uploaded
      properties:
        album_id:
          type: integer
          format: int32
        cover_uploaded:
          type: boolean

    AlbumInfoResponse:
      type: object
      required:
        - album_id
        - album_name
        - tracks
        - release_year
        - release_full_date
      properties:
        album_id:
          type: integer
          format: int32
        album_name:
          type: string
        release_year:
          type: integer
          format: int32
          nullable: true
        release_full_date:
          type: string
          format: date
          nullable: true
        tracks:
          type: array
          items:
            type: integer
            format: int32

    AlbumDeleteResponse:
      type: object
      required:
        - album_id
      properties:
        album_id:
          type: integer
          format: int32

    AlbumCoverResponse:
      type: object
      required:
        - album_id
      properties:
        album_id:
          type: integer
          format: int32

security:
  - bearerAuth: []
